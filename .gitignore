
*~
*.a
# Additional build output dirs (unanchored)
.angular/cache/
*.apikey
# Auto-added ignores (2025-09-30T03:50:24+09:00)
.aws/
.azure/
*.bak
bin/
# Binary build artifacts
build/
# Build artifacts (fast to regenerate) — ignore
# Build outputs (generic)
.cache/
# C/C++/CMake
# Chandra/ciao raw event trees under cluster data
checkpoints/
# CMake
cmake-build-*/
CMakeCache.txt
CMakeFiles/
# Consolidated local ignore (migrated from ~/.gitignore_global)
.coverage
coverage/
# Coverage artifacts
coverage.xml
*.crt
# CSV and intermediate tables are ignored
data/
**/data/cluster/**/ciao/
**/data/cluster/**/primary/
**/data/cluster/**/repro/
**/data/cluster/**/secondary/
# Data (large/generated)
Desktop.ini
dist/
*.dll
/docs/_build/
.DS_Store
*.dylib
# Editors/IDEs
ehthumbs.db
# Ensure logs and *.log inside reports are not ignored by generic rules
.env
.env/
# Environments and secrets
*.exe
# Figures generated on the fly in reports (keep images tracked via LFS)
.gcp/
.gradle/
# High-churn runtime outputs kept outside git (archived snapshots in archives/)
# HTML pages are deliverables — keep
# IaC
.idea/
id_ecdsa*
id_ed25519*
id_rsa*
.ipynb_checkpoints/
# Java/Gradle/Maven
# JS bundlers
# JS bundlers (unanchored)
# Keep heavy deliverables (long runs) — include via negation
*.key
KiDS-450_COSMIC_SHEAR_DATA_RELEASE/
.kube/
# Language/tool caches (already have Python; add more)
lightning_logs/
*.log
**/logs/
.mamba/
memo/private/
# ML experiment outputs
# ML experiments
mlruns/
.mvn/
.mypy_cache/
.netrc
.next/
node_modules/
# Node/npm
# Node/process pid/meta if any
.nox/
.npm/
.nuxt/
.nyc_output/
*.o
obj/
*.orig
# OS files
# OS trash
*.out
out/
# Override: include everything under server/public/reports
*.p12
paper/figures/*.png
paper/figures/*.svg
.par
.parcel-cache/
.pdm-cache/
*.pem
*.pfx
*.pid
.pnpm-store/
/public/
*.pyc
__pycache__
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
# Python caches
# Python caches and temp
# Raw external datasets (keep out of repo)
# Reports: JSON outputs are ignored by default, except for heavy deliverables listed below
# Reports: keep everything except logs (*.log) explicitly
# Root-only static site builds (avoid ignoring server/public)
# Root-only static site builds (prevent collision with server/public)
.ruff_cache/
runs/
secrets.*
# Secrets/credentials — never commit
# Secrets (never commit)
server/certs/
server/public/notifications/
!server/public/reports/
!server/public/reports/**
!server/public/reports/bench_*.json
server/public/reports/cluster/
!server/public/reports/data_catalog.json
!server/public/reports/*_holdout.json
server/public/reports/**/*.log
server/public/reports/logs/
!server/public/reports/ws_vs_phieta_summary.json
server/public/site-lastmod.js
server/public/state_of_the_art/audit*.json
server/public/state_of_the_art/holdout_progress.html
server/public/state_of_the_art/jobs.html
server/web.log
/_site/
site/
# Site/doc builds (rebuildable)
*.so
.ssh/
.storybook-static/
.svelte-kit/
*.swo
*.swp
target/
*.temp
# Temp/swap/backup files
.terraform/
.terragrunt-cache/
Thumbs.db
*.tmp
/tmp/
tmp/
*.token
.tox/
.Trashes
.turbo/
.vagrant/
# Vagrant
.venv/
/.venv/
# Virtualenv and caches
# Virtual envs
.vite/
.vscode/
wandb/
.yarn/
