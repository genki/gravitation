## Tuned config for SOTA refresh (asinh + knee + multiscale)

# Training objective weight for negative Spearman encouragement (higher → stronger push negative)
W_CORR_FRAC=0.25

# Σ_e transform and multi-scale interface gating
SE_TRANSFORM=asinh
GATE_SIGMAS=2,4,8

# Interface gate strength and band width (thicker, stronger)
GRID_S_GATE=32,40,48
# Allow script default (0.70,0.75,0.80); keep 0.65 for exploratory runs
GRID_TAUQ=0.65

# W_eff shaping: emphasize high-Σ_e, saturate low/mid
GRID_Q_KNEE=0.6
GRID_XI=2.4,3.6
GRID_XI_SAT=0.3333
GRID_P=1.0

# Beta search grid during constrained search
GRID_BETA=0.8,0.9,1.0

# Holdout settings (high rigor)
BETA_SWEEP=0.8,0.9,1.0
PSF_LIST=1.2,1.5,1.8
BULLET_PERM_N=5000
BULLET_BOOT_N=4096
