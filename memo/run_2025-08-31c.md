# 用語注記(サーバ)と進捗率更新

- 実行日: 2025-08-31
- 目的: SOTAサイトにFDBの正式名称を明記し、進捗率を記録。

## 実施内容
- `server/server.py` のSOTAページに注記を追加:
  - 「FDB=Future Decoherence Bias（ULW-EM起因の見かけ重力）。
     FDB3=3パラメータ型、FDBL=3D拡張型」
- 進捗率を更新: `RATE=78`, NOTE="FDB定義統一: サーバ注記/論文本・docs反映"

## 結果サマリ
- 用語の統一がWeb UIにも反映され、SOTA閲覧者がFDB系列の意味を
  即時把握できる状態に。進捗は78%に更新。

## 生成物
- `server/server.py`（注記追加）
- `data/progress.json`（新レコードを追記）

## 次アクション
- mkdocsサイトの再ビルド/公開が必要なら `make docs`/`publish-site` を実行。
- FDB定義注記を他の固定ページ(トップ/メモ一覧)にも短文で追補（任意）。

