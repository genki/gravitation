# サイト更新（SOTA/図同期/MkDocs再ビルド/サーバ再起動）

- 実行日: 2025-09-01
- 目的: 直近の図とSOTA更新を反映し、サイトが未更新の状態を解消。

## 実行
- スクリプト: `scripts/update_and_build_site.sh` を改修（venv版mkdocs優先）。
- 手順: 図生成→SOTA最終更新日の反映→`docs/img`へ同期→`mkdocs build`。
- 公開系: `make web-deploy` で SOTA静的再生成 + TLSサーバ再起動。

## 結果サマリ
- MkDocsサイトを `site/` に再生成（Materialテーマ）。
- `server/public/state_of_the_art/index.html` を再生成。
- TLSローカルサーバを再起動し、最新に更新（https://localhost:3131）。

## 生成物
- 静的サイト: `site/` 一式
- 公開: `server/public/state_of_the_art/index.html`, `server/public/assets/*`
- 改修: `scripts/update_and_build_site.sh`（venv対応）

## 次アクション
- 必要に応じて `publish-site` のCI連携（任意）を検討。
- 代表図のΔVパネル高さを一括再生成に反映するオプション追加（任意）。

