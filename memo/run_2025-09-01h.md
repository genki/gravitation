# SOTAのM/L一覧ページの補完と列拡充

- 実行日: 2025-09-01
- 目的: 「全銀河のM/L一覧（別ページ）」に期待どおりの一覧が出ない問題の解消。
  併せてDisk/BulgeのM/L列も追加し情報量を拡充。

## 実行
- `scripts/build_state_of_the_art.py` を更新:
  - テーブル生成で `assets/results/global_fit_ml.csv` を取り込み、
    列に Disk M/L / Bulge M/L を追加。
  - 既存の GR μ / ULW μ と併記（無い場合は '-' 表示）。
- 再生成: `make web-deploy` で `/state_of_the_art/table.html` を更新。

## 結果サマリ
- ページ `/state_of_the_art/table.html` に M/L 一覧が表示され、
  さらに Disk/Bulge の列を追加（例: CamB, DDO154 等を確認）。

## 生成物
- 修正: `scripts/build_state_of_the_art.py`
- 出力: `server/public/state_of_the_art/table.html`

## 次アクション
- MkDocs 側にも一覧ページを複製する `docs/ml-table.md` の追加を検討（任意）。
- 列の並びやCSV出力(ダウンロード用)の追加も対応可能。

