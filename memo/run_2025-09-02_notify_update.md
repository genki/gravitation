# タスク完了時の通知運用の明文化（AGENTS.md更新）

- 実行日: 2025-09-02
- 目的: すべてのタスク完了時に通知を送ることを明文化し、実オペでの抜け漏れを防止。

## 実行
- `AGENTS.md` に「Agent必須ルール」を追記。
  - 既定は `make notify-done-site`（通知+サイト更新）。
  - サイト更新が不要な場合のみ `make notify-done` を使用。
- 運用: 作業要点を `memo/run_*.md` に残せば、通知本文は自動抽出される。

## 結果サマリ
- 通知運用の必須化を明記。
- 以後のタスクでは終了時に確実に通知される。

## 生成物
- ドキュメント: `AGENTS.md`（Agent必須ルールを追加）

## 次アクション
- 必要に応じてCIで `notify-done-site` の実行をフック（任意）。
