# run_2025-09-24_abells1063_mask075_psf15_hp15_w0_n2k

## 結果サマリ
- `scripts/reports/make_bullet_holdout.py` に `BULLET_PERM_MIN` を尊重する分岐を追加し、`sweep_bullet_shadow.py` からも同値を渡せるよう調整。これにより residual permutation を 512 程度に抑えた検証が可能になった。
- `σ_psf=1.5, σ_highpass=1.5, mask_q=0.75, w=0` (ログ `logs/abells1063_hp15_psf15_w0_perm512.log`) を 2 候補×512 permutation で実行したが、`S_shadow(global)=0.0547`, `outer=0.146`, `p_perm=0.43` と有意水準未達のまま。ΔAICc は `FDB−rot=-1.364×10^7`, `FDB−shift=-6.19×10^5` を維持。
- `σ_highpass=1.0, se_q=0.70` へ変更し mask_q=0.75 (`logs/abells1063_hp10_psf15_w0_perm512.log`) および mask_q=0.70 (`logs/abells1063_hp10_psf15_w0_mask70_perm512.log`) を検証したが、いずれも同じ `S_shadow(global)=0.0547`, `p_perm≈0.43` に収束。旧スイープで得た `S_shadow≈0.20–0.38` は最新 shared params では再現できず、候補の最上位が edge_count=256 ケースに固定されていると判明した。
- 512 permutation でも StageResume により residual permutation が自動スキップされ、8〜9 分程度で 1 組の候補評価が完了する体制を整えた。

## 生成物
- 実行ログ: `logs/abells1063_mask075_psf15_hp15_w0_n2k.log`, `logs/abells1063_hp15_psf15_w0_perm512.log`, `logs/abells1063_hp10_psf15_w0_perm512.log`, `logs/abells1063_hp10_psf15_w0_mask70_perm512.log`
- 更新: `scripts/reports/make_bullet_holdout.py`, `scripts/reports/sweep_bullet_shadow.py`
- 更新: `server/public/reports/cluster/AbellS1063_holdout.json` （最新 run）、進捗ログ `server/public/reports/cluster/AbellS1063_progress.log`

## 次アクション
1. edge_count=256 が恒常的に選ばれる原因を調査し、`edge_count`/`edge_quantile` の探索ロジックを調整して高 S_shadow 値の候補を再度拾えるようにする（必要なら `BULLET_EDGE_COUNT` による disable フラグを追加実装）。
2. `weight_power∈{0.3,-0.3}` や `block_pix=8` を含むスイープを 512 permutation 体制で回し、outer ROI の S_shadow 強化に繋がるパターンを洗い出す。
3. 高 S 候補が再現できた場合は perm=12,000 での本番検証に昇格させ、SOTA 連携用に `make sota-refresh` を予定。
