## 結果サマリ
- すべてのログファイル（`*.log`）と `**/logs/` ディレクトリを gitignore に追加し、既にインデックスされた `.log` を除去しました。
- 公開成果（HTML/PNG/SVG/JSON）やアーカイブ（LFS）は引き続き管理対象です。

## 生成物
- 更新: `.gitignore`
- 変更: 追跡から外したログ（`server/public/reports/final_2025-09-15/logs/*.log` など）

## 次アクション
- 必要に応じて、特定のログをアーカイブ（LFS）で残す運用に追加可能です。
