## 結果サマリ
- FDB 優位性“即示”論文の 8 ページ構成を paper2/main.md に集約し、各節の骨子・数式・図表案・QC 注意点を整理した。
- H1 比テスト手順を v_c 軸（2π 版）で明記し、PASS 基準・サーベイ別評価・図表仕様を記述した。
- BTFR/AICc、界面全反射、Proca 質量スケールなど既存ドラフトの要点を一本化し、即時検証性を前面に出した結論部を作成した。
- arXiv 用ビルド環境として paper2/Makefile を追加（pandoc で PDF/TeX 生成）、図表プレースホルダ PNG を paper2/figures/ に配置し、main.md から参照。
- pandoc + TeX 環境（texlive + xetex）を導入し、`make -C paper2 pdf` / `tex` を通して build/main.pdf, build/main.tex を生成（記号置換により警告解消済）。
- 表記を「Future Determination Bias」に改名し、著者名を Genki Takiuchi に設定してビルドを更新。
- 査読コメントに対応し、2π/4π 正規化の Box 追加、H1 比テストの即時判定文、ΔAICc 中央値/IQR の明示、界面モード選択と貫入長の整合を Appendix A に追記。
- H1 図を実データバイオリン（survey別）に差し替え、Appendix C を 2π 正規化に訂正。Appendix D（エネルギー上限）と Appendix F（再現手順）を追加し、refs.bib を組み込んで再ビルド。
- 冒頭の体裁メモ（arXiv draft〜など）を削除し、本体記述のみ残した。
- §4 に時計モデルと dτ/dt、情報ポテンシャルでの保存力定義を追加し、強重力節を新設。赤方偏移の軽量テストを Appendix F として追加、H1 再現手順を Appendix G に移動。
- SPARC 再計算（固定 M/L=0.5/0.7、誤差床5 km/s、FDB k=1、NFW k=2）を実施し、ΔAICc 全件 CSV を `paper2/build/sparc_aicc.csv` として生成。BTFR 図・NGC6503 回転曲線を実測データで差し替え、Table2 サマリを更新した。
- 著者行の “Formerly ...” 注釈を削除し、タイトルまわりを簡潔化。
- BOSS については Re 未知を区間補正で扱い、主判定は SDSS+BELLS と明記。本文 5.1 に ΔAICc 符号定義と外縁AICc/Δv²傾きの結果（all/outer）を記載。LaTeX グリフ警告（≥）をエスケープして解消。
- BOSS/BELLS+SL2S のフル表を `data/strong_lensing/BOSS_full_table.csv` に追加し、H1 再計算（N=267, BOSS N=72）。m_R/s_R を本文に反映し、H1 図は実データのまま。

## 生成物
- paper2/main.md: arXiv 想定 1 カラム ~8p の Markdown ドラフト（Abstract〜Appendix、図表チェックリスト付き）。
- paper2/Makefile: `make pdf` / `make tex` で build/main.* を生成。
- paper2/figures/*.png: 図5枚（interface, flux field, rotcurve_NGC6503 実測, BTFR_sparc 実測, H1バイオリン）を更新。
- paper2/build/main.pdf, paper2/build/main.tex: pandoc+xelatex で出力した現時点のビルド成果。
- paper2/build/sparc_aicc.csv, paper2/table2_aicc.md: SPARC ΔAICc 全件とサマリを格納。
- data/strong_lensing/BOSS_full_table.csv: 文献表を取り込み、H1 に反映。

## 次アクション
- ΔAICc 全件表を Table2 に差し込み、SPARC 図を実データに差し替え。引用整理と付録の式番号・本文参照の同期。
- ApJ/PRD 体裁の最終整形とリファレンス拡充。
