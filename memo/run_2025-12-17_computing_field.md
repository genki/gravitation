# 2025-12-17 計算場宇宙論ページ追加

## 結果サマリ
- 指定本文を `computing-field.md` としてリポジトリに追加。
- 同内容の Wiki ページ `Computing-Field` を追加し、Home からリンクを追加。

## 主要変更点
- 追加: `computing-field.md`
- Wiki: `Computing-Field.md`（新規）、`Home.md`（リンク追加）

## 次の一手
- wiki→Pages の自動生成が走った後、GitHub Pages 上で `Computing-Field` が出力されることを確認。
- もし即時反映が必要なら、手動で `gh-pages` を再生成して push する。

## 通知
- `make notify-done` が Makefile に無く、自動通知手順は実行できない（代替手段の整備が必要）。
