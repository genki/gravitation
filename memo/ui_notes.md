# Web UI整備メモ

- 目的: レイアウトとナビゲーションの改善。
- 変更点:
  - ヘッダーに固定ナビ(ホーム/メモ/データ/論文)。
  - `server/public/styles.css`を追加し、共通スタイル化。
  - トップをカード型で整理し、一覧ページ(memos/data/paper)を
    追加。
- 方針:
  - 行幅80桁内でソースを書く(HTML/CSS改行を細かく)。
  - 依存なし(プレーンHTML/CSS)。
- TODO:
  - サーチ導線(簡易フィルタ)の追加検討。
  - 更新日時の自動表示(将来、生成スクリプト導入時に対応)。

